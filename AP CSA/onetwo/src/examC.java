/**
 * Created by Administrator on 10/11/2017.


public class examC {
    public static void main(String [] args){
        /*int x = 10, y = 8;
        if (x >= y/2)
            if (x >= y*2)
                x = x + 1;
            else
                y = y + 1;
             System.out.println(x);
        System.out.println(y);
        boolean car = true;
        boolean truck = false;
        boolean bike = true;
        double cost = 0.0;
        if (truck)
            cost = 22000;
        else if (car)
            cost = 18000;
        else if (car && bike)
            cost = 18500;
        else
            cost = cost + 10000;
        System.out.println(cost);
*

        boolean x = false;
        boolean y = false;
        boolean z = true;
        boolean a = (x == y && x != z) || (x !=y && x == z);
        boolean b =(x == y || x == z) && (x !=y || x != z);
        boolean c =(x == y) != (x == z);
        System.out.println(a);
        System.out.println(b);
        System.out.println(c);
        
        boolean x = true;
        boolean y = true;
        boolean z = true;
        boolean a = (x && !y || z) || !z && x;
        System.out.println(a);

        int d = 40;
        switch (d) {
            case 20:
                d += 10;
            case 40:
                d -= 10;
            case 60:
                d *= 10;
            default:
                if (d > 100) {
                    d -= 100;
                }
        }
        System.out.println(d);


        int w=8;
        int p=11;
        boolean wp= !((w >= p) && (w != 1));
        boolean pw= (w < p) && (w == 1);
        System.out.println(wp);
        System.out.println(pw);

        
        boolean x>=y = true;
        boolean x != 1 = true;
boolean x<y = true;
boolean x==1 = true;
boolean x<=y = true;
boolean x=1 = true;



        boolean a = !((x >= y) && (x != 1);
        boolean b=(x < y) && (x == 1);
        boolean c= (x <= y) || (x == 1);
        boolean d= (x >= y) || (x != 1);
         boolean e= (x >= y) || (x = 1);
         boolean f=(x < y) || (x == 1);
    }
}
**/